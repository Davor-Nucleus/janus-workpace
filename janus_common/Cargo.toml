[package]
name = "janus_common"
version = "0.1.0"
edition = "2021"

[dependencies]
winapi = { version = "0.3", features = ["wincon", "winuser", "libloaderapi", "commctrl", "wingdi"] }
widestring = "1"
