[package]
name = "janus_config_nucleus"
version = "0.1.0"
edition = "2021"

[dependencies]
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
janus_log_nucleus = { path = "../janus_log_nucleus" }
